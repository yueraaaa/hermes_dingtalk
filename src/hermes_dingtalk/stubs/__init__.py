"""Stub interfaces for Hermes gateway internals.

These are minimal definitions that mirror the Hermes Agent internal APIs
used by the DingTalk adapter. They allow the adapter to run and be tested
independently, without requiring the full Hermes Agent codebase.

When used within Hermes Agent, the real implementations from
``gateway.config``, ``gateway.platforms.base``, etc. take precedence.
"""
